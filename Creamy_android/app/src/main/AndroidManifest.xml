<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ko_desk.myex_10">

    <!--추가시작-->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--추가종료-->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="CreamyCRM"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.SignInActivity"> <!-- 변경 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--추가시작-->
        <activity android:name=".Activity.MyPageMainActivity" />
        <activity android:name=".Activity.MyPageUserActivity"/>
        <activity android:name=".Activity.JoinActivity" />
        <!--추가종료-->

    </application>

</manifest>